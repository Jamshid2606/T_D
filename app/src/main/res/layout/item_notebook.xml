<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="top|start"
    android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/iconNote"
        android:layout_width="200dp"
        android:layout_height="200dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>
    <TextView
        android:id="@+id/nameNote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/iconNote"
        app:layout_constraintStart_toStartOf="@id/iconNote"
        android:text="Ingliz tili"
        android:textSize="16sp"
        android:textColor="@color/black"
        android:fontFamily="@font/inter"/>
    <ImageView
        android:id="@+id/popupIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/more"
        app:layout_constraintEnd_toEndOf="@+id/iconNote"
        app:layout_constraintTop_toTopOf="@+id/nameNote"/>

</androidx.constraintlayout.widget.ConstraintLayout>